{
  "name": "subagent-bouncer",
  "version": "0.2.0",
  "description": "Stops Claude Code subagents from silently billing at your session model's rate — denies unrouted Agent spawns and workflow agent() calls; the deny reason makes the session model re-issue on the cheapest capable tier.",
  "author": {
    "name": "Juno Seong"
  }
}
